{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "sourceLanguages": [
        "swift"
      ],
      "args": [],
      "cwd": "${workspaceFolder:windows-samples}",
      "name": "Debug WindowsApp",
      "program": "${workspaceFolder:windows-samples}\\.build\\debug\\WindowsApp.exe",
      "preLaunchTask": "swift: Build Debug WindowsApp"
    },
    {
      "type": "lldb",
      "request": "launch",
      "sourceLanguages": [
        "swift"
      ],
      "args": [],
      "cwd": "${workspaceFolder:windows-samples}",
      "name": "Release WindowsApp",
      "program": "${workspaceFolder:windows-samples}\\.build\\release\\WindowsApp.exe",
      "preLaunchTask": "swift: Build Release WindowsApp"
    }
  ]
}
